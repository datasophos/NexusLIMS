repos:
  - repo: local
    hooks:
      - id: lint
        name: Run linting checks
        entry: /opt/homebrew/bin/uv --help
        language: unsupported
        stages: [pre-commit]
        pass_filenames: false
        always_run: true
