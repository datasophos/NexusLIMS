Added a `nexuslims support-bundle` command that creates a local diagnostic archive with redacted configuration, preflight results, database summaries, selected logs, and environment details for Datasophos support.
