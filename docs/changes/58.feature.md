NexusLIMS is now fully installable as a Python package via `pip install nexusLIMS` or `uv tool install nexusLIMS`. The package includes all required schema files, database migrations, and CLI commands. A comprehensive smoke test validates the installed package on every release.
