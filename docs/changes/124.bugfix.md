Aligned export destination preflight and final session status handling with NX_EXPORT_STRATEGY so unavailable destinations now stop builds when the configured strategy cannot succeed, and partial exports under the default all strategy are no longer marked completed.
