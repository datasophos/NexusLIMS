Fixed module-level settings access in `nexusLIMS/harvesters/__init__.py` that caused validation errors when importing NexusLIMS code without configuring `.env`. Certificate authority bundle configuration is now loaded lazily via `get_ca_bundle_path()` and `get_ca_bundle_content()` functions, allowing modules to be imported before environment variables are set while still providing helpful error messages when configuration is actually accessed.
