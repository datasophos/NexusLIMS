Replaced custom nested dictionary utility functions with the well-maintained `python-benedict` library. This improves code reliability and maintainability by using battle-tested implementations for nested dictionary operations ({func}`~nexusLIMS.utils.set_nested_dict_value`, {func}`~nexusLIMS.utils.get_nested_dict_value_by_path`, {func}`~nexusLIMS.extractors.flatten_dict`). Removed vestigial functions `get_nested_dict_key` and `get_nested_dict_value` that had minimal or no production usage.
